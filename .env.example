# Worker URL for the /status page (safe to expose — baked into built HTML)
PUBLIC_STATUS_API_URL=https://lqi-status.lowqualityinteractive.workers.dev

# YouTube Data API key for subscriber count on /connect (keep secret — do NOT commit)
YOUTUBE_API_KEY=
