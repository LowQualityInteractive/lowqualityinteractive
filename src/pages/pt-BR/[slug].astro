---
import type { GetStaticPaths } from 'astro';
import { getGameAboutStaticPaths } from '../../data/games';
import GameAboutPage from '../../views/GameAboutPage.astro';

export const getStaticPaths: GetStaticPaths = () => getGameAboutStaticPaths();

interface Props {
  slug: string;
}

const { slug } = Astro.props;
---

<GameAboutPage locale="pt-BR" slug={slug} />
